fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
chromadb==0.5.23
httpx==0.28.1
PyPDF2==3.0.1
python-docx==1.1.2
openpyxl==3.1.5
cryptography==44.0.0
python-dotenv==1.0.1
PyJWT==2.10.1

# ============================================================
# Forecast 时序预测推理服务依赖（运行于 Python 3.11，非后端 Python 3.9）
# 这些包不安装到后端环境，仅供 backend/scripts/chronos_server.py
# 和 backend/scripts/timesfm_server.py 使用
# 安装命令（Python 3.11）：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install chronos-forecasting timesfm pandas fastapi uvicorn pydantic
# 注意：timesfm 2.5 要求 Python >=3.10，不可在 Python 3.9 安装
# ============================================================
